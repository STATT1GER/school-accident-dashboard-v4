# 학교안전 사고경로 관제 대시보드 v4 설계 명세

## 1. 개편 목적

핵심 질문은 다음과 같다.

> 어떤 시간에, 어떤 장소에서, 어떤 활동을 하다가, 어떤 사고형태로 이어지는가?

사고경로를 화면의 핵심 정보로 두고 시간·장소·활동 분석은 경로를 해석하고 개입 지점을 찾기 위한 보조 근거로 사용한다. 특정 날짜나 개인의 사고를 예측하는 모델이 아니다.

## 2. 전역 원칙

- 핵심 분석 단위: `사고시간 → 사고장소 → 사고 당시 활동 → 사고형태`
- 화면 기본 표현: `사고 반복 경로`, `공간적 개입 우선순위`, `상대 사고집중도`
- 실제 원자료와 층 정보 미사용
- 심각도와 임의 가중합 위험지수 미사용
- 합성 데이터와 가상학교 1층 지도임을 명시

상대 사고집중도는 다음과 같이 계산한다.

```text
상대 사고집중도 = 해당 범주의 사고 건수 ÷ 선택 조건 내 최대 사고 건수 × 100
```

이는 실제 사고확률이나 개인의 위험도를 의미하지 않는다.

## 3. 메인 화면

- 핵심 문구: `단순 사고 건수 비교가 아닌, 사고가 발생하는 경로의 흐름을 탐색합니다.`
- 사고 반복 경로라는 간결한 표현 사용
- `2개 관제 화면` 표시
- `오늘의 학교 안전상황` 카드 제거
- 학교지도와 사고분석을 2열 카드로 배치
- 흐름: 공간적 개입 우선순위 확인 → 사고 반복 경로 분석 → 추천 개입 지점 도출

## 4. 학교지도

- `assets/school_map.png`의 가상학교 1층 지도만 사용
- 층 선택과 층 필터 제거
- 일과 구간 선택만 제공
- 사고장소 범주를 지도상의 고정 앵커와 결정적 jitter로 재매핑
- 히트맵: 시연 좌표의 밀도
- 마커 크기: 사고 시연 건수
- 마커 색: 선택 조건 내 상대 사고집중도 0~100
- 지도 좌표는 실제 사고 위치가 아님을 명시
- 공간별 순위 명칭은 `공간적 개입 우선순위`

## 5. 사고분석

### 5.1 탭 순서

```text
사고경로 → 시간 분석 → 장소 분석 → 활동·형태
```

### 5.2 상단 KPI

1. 분석 사고 신호
2. 핵심 위험시간
3. 핵심 위험장소
4. 대표 사고경로 비중

핵심 시간과 장소는 최빈 4단계 경로의 첫 번째와 두 번째 단계에서 산출한다.

### 5.3 Sankey

- 전체·저학년·고학년 탭 유지
- 간단히 보기와 전체 범주 보기 유지
- 노드 ID는 `(단계, 범주)`로 구분
- 동일 노드 라벨은 한 번만 표시
- 긴 라벨은 줄바꿈
- 단계별 X 좌표와 노드별 Y 좌표를 고정하여 겹침 완화
- hover에서는 원래 전체 라벨 표시

### 5.4 추천 개입 지점

기존 `경로 상세 탐색`과 selectbox를 제거한다.

대신 최빈 사고경로를 기준으로 다음을 표시한다.

- 핵심 위험시간
- 핵심 위험장소
- 우선 개입 시점
- 대표 활동과 사고형태
- 활동 유형별 규칙 기반 추천 조치

### 5.5 시간 분석

- 시간대별 상대 사고집중도
- 요일 × 사고시간 상대 사고집중도
- 실제 사고확률이 아니라는 안내문 표시

### 5.6 장소 분석

- 층별 분포 삭제
- 사고장소 막대그래프 전체 너비
- 장소별 사고건수, 비율, 대표활동, 대표사고형태 제공

### 5.7 활동·형태

1. 사고장소 선택
2. 선택 장소의 활동 분포
3. 활동 선택
4. 선택 활동의 사고형태 분포

## 6. 화면 구성

```text
메인
├── 학교지도
└── 사고분석
```

`오늘의 학교 안전상황` 페이지는 삭제하며 남은 페이지 파일은 다음과 같다.

```text
pages/1_학교지도.py
pages/2_사고분석.py
```

## 7. 실행환경

```powershell
uv sync
uv run python -m streamlit run app.py
```
